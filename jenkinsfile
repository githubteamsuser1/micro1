pipeline {
    agent any

    stages {
        stage('Download Code') {
            steps {
                echo 'Inside Micro 1'
                sh 'printenv'
            }
        }
    }
}
