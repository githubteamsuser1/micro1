pipeline {
    agent any

    stages {
        stage('Download Code') {
            steps {
                echo 'Inside Micro 1'
                echo 'Pulling... ' + env.GIT_BRANCH
            }
        }
    }
}
